{
  "scripts": {
    "build": "tailwindcss -i tailwind.css -o src/styles.css --minify",
    "watch": "tailwindcss -i tailwind.css -o src/styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
